import React, { useEffect, useState } from 'react';

const Item = (props) => {
	const { imageUrl, name, collectionName, description, permalink } = props;
	return <div>123</div>;
};

export default Item;
